services:
  - type: web
    name: hope-to-reach
    runtime: static
    buildCommand: echo "Static website ready"
    staticPublishPath: ./
    headers:
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /*
        name: Referrer-Policy
        value: strict-origin-when-cross-origin
